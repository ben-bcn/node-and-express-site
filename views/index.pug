extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 Ben's Portfolio

      p.lead.text-light
        | This is where a lot of stuff is posted about all the myriad of great things Ben does on a daily basis.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each val in projects
        .cell
          a(href=`${val.live_link}`)
            img.thumbnail(src=`${val.hero_img}`)
            h5= val.project_name
